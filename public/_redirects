# Redirect API calls to backend (will be configured in Render dashboard)
# This file is a placeholder - actual rewrites will be configured in Render Static Site settings

# SPA fallback - serve index.html for all routes
/*    /index.html   200
